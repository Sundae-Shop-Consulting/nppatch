<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Membership_Status__c</fullName>
    <description>Formula field. Returns Current, Grace Period, or Expired based on Membership End Date and the grace period configured in Household Settings (default 30 days).</description>
    <externalId>false</externalId>
    <formula>if ( MembershipEndDate__c  &lt;today(), if (MembershipEndDate__c  &gt; ( TODAY() -
IF(NOT(ISNULL($Setup.Households_Settings__c.Membership_Grace_Period__c)), $Setup.Households_Settings__c.Membership_Grace_Period__c, 30)) , "Grace Period" , "Expired") , if(isnull(MembershipEndDate__c ),"","Current"))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Whether this Household's membership is Current, in a Grace Period, or Expired.</inlineHelpText>
    <label>Membership Status</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
