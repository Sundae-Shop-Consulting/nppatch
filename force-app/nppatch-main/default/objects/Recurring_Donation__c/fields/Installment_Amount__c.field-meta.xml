<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Installment_Amount__c</fullName>
    <description>Formula field. Calculates the per-installment Opportunity amount. For Open or Multiply By schedules, equals the Amount field. For Divide By schedules, divides Amount by Number of Planned Installments.</description>
    <externalId>false</externalId>
    <formula>IF(

OR(
(text(Schedule_Type__c)=$Label.RecurringDonationMultiplyValue),
(text(Open_Ended_Status__c)=$Label.RecurringDonationOpenStatus)
)
, Amount__c , IF(Installments__c&gt;0,Amount__c / Installments__c,0)




)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>The calculated amount for each individual donation installment, based on the Amount and Schedule Type settings.</inlineHelpText>
    <label>Installment Amount</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
