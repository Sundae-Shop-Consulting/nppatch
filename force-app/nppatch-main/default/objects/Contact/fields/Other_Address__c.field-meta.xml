<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Other_Address__c</fullName>
    <description>Formula field. Assembles the Contact's Other address based on Primary and Secondary Address Type fields. Pulls from Mailing or Other address fields accordingly.</description>
    <externalId>false</externalId>
    <formula>IF(
  ISPICKVAL(%%%NAMESPACE%%%Primary_Address_Type__c,"Other"),
  IF(ISBLANK(MailingStreet), "", MailingStreet &amp; ", ") &amp;
  IF(ISBLANK(MailingCity), "", MailingCity &amp; ", ")&amp;
  IF(ISBLANK(MailingState), "", MailingState &amp; " ")&amp;
  IF(ISBLANK(MailingPostalCode), "", MailingPostalCode) &amp;
  IF(ISBLANK(MailingCountry), "", ", " &amp;MailingCountry)
,
IF(ISPICKVAL(%%%NAMESPACE%%%Secondary_Address_Type__c,"Other"),
  IF(ISBLANK(OtherStreet), "", OtherStreet &amp; ", ") &amp;
  IF(ISBLANK(OtherCity), "", OtherCity &amp; ", ")&amp;
  IF(ISBLANK(OtherState), "", OtherState &amp; " ")&amp;
  IF(ISBLANK(OtherPostalCode), "", OtherPostalCode) &amp;
  IF(ISBLANK(OtherCountry), "", ", " &amp; OtherCountry)
  ,""
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This Contact's Other address, derived from the Mailing or Other address based on the Primary Address Type.</inlineHelpText>
    <label>Other Address</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
