<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Organization_Type__c</fullName>
    <description>Formula field. Returns the Account type classification for this Contact: checks SYSTEM_AccountType first, then Account.Type, defaulting to 'Organization'. Used internally to determine processing paths for household vs. organizational accounts.</description>
    <externalId>false</externalId>
    <formula>NULLVALUE(IF( Account.%%%NAMESPACE%%%SYSTEM_AccountType__c=="", TEXT(Account.Type),Account.%%%NAMESPACE%%%SYSTEM_AccountType__c),"Organization" )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>The type of Account this Contact belongs to (e.g., Household Account, Organization). Determined by system fields.</inlineHelpText>
    <label>Organization Type</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
