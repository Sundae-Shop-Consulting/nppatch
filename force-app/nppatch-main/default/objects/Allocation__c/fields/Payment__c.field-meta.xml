<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Payment__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Links this Allocation to a specific Payment rather than the parent Opportunity. Used when allocations need to differ between Payments on the same Opportunity. Either Payment or Opportunity should be set, not both.</description>
    <externalId>false</externalId>
    <inlineHelpText>The Payment this allocation applies to. Use this instead of the Opportunity field when different Payments on the same Opportunity need different fund designations.</inlineHelpText>
    <label>Payment</label>
    <referenceTo>OppPayment__c</referenceTo>
    <relationshipLabel>Payment Allocations</relationshipLabel>
    <relationshipName>Allocations</relationshipName>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
